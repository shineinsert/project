//import { appConfig } from 'config';

//export function prepareBaseServerUrl(path: string): string {
//  return appConfig.basePath + path.replace(/\\/g, "/");
//}

//export function convertPathToStaticUrl(baseUrl: string, path: string): string {
//  return baseUrl.replace(/\/admin/g, "") + path.replace(/\\/g, "/");
//}
